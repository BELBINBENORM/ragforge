name: ragforge

services:
  api:
    build: .
    image: ragforge:latest
    container_name: ragforge
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - documents_data:/app/data/documents
      - logs_data:/app/logs

volumes:
  documents_data:
  logs_data: